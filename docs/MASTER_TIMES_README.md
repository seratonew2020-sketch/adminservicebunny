# ระบบบันทึกเวลาเข้างาน (Master Times System)

## 📋 สารบัญ

1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [สถาปัตยกรรมระบบ](#สถาปัตยกรรมระบบ)
3. [กฎการทำงาน](#กฎการทำงาน)
4. [โครงสร้างฐานข้อมูล](#โครงสร้างฐานข้อมูล)
5. [API Endpoints](#api-endpoints)
6. [การติดตั้งและใช้งาน](#การติดตั้งและใช้งาน)
7. [การทดสอบ](#การทดสอบ)
8. [ตัวอย่างการใช้งาน](#ตัวอย่างการใช้งาน)

---

## 🎯 ภาพรวมระบบ

ระบบบันทึกเวลาเข้างาน (Master Times) เป็นระบบที่ออกแบบมาเพื่อจัดการและประมวลผลข้อมูลการเข้า-ออกงานของพนักงาน โดยรองรับการทำงานในหลายช่วงเวลา รวมถึงกะทำงานที่ข้ามวัน

### ✨ คุณสมบัติหลัก

- ✅ รองรับ 4 ช่วงเวลาทำงานหลัก (Master Shift Times)
- ✅ ประมวลผลบันทึกเวลาอัตโนมัติ
- ✅ จัดการกะทำงานข้ามวัน (Overnight Shifts)
- ✅ ตรวจจับการลืมสแกนเข้า-ออกงาน
- ✅ คำนวณชั่วโมงทำงานอัตโนมัติ
- ✅ สร้างรายงานสรุปการเข้างาน
- ✅ แสดงสถิติแบบ Real-time

---

## 🏗️ สถาปัตยกรรมระบบ

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (Vue 3)                     │
│  - Vuetify UI Components                                │
│  - Pinia State Management                               │
│  - Axios HTTP Client                                    │
└──────────────────┬──────────────────────────────────────┘
                   │
                   │ REST API
                   │
┌──────────────────▼──────────────────────────────────────┐
│              Backend (Node.js + Fastify)                │
│  - API Routes (masterTimes.js)                          │
│  - Business Logic (masterTimesService.js)               │
│  - Supabase Client                                      │
└──────────────────┬──────────────────────────────────────┘
                   │
                   │ PostgreSQL
                   │
┌──────────────────▼──────────────────────────────────────┐
│                Database (Supabase)                      │
│  - master_shift_times                                   │
│  - attendance_logs                                      │
│  - processed_attendance                                 │
│  - v_daily_attendance_summary (View)                    │
└─────────────────────────────────────────────────────────┘
```

---

## 📜 กฎการทำงาน

### 1. ช่วงเวลาทำงานหลัก (Master Shift Times)

| ช่วงที่ | เวลาเข้างาน | เวลาออกงาน | ประเภท |
|---------|-------------|------------|--------|
| 1 | 10:00 | 19:00 | ปกติ |
| 2 | 11:00 | 20:00 | ปกติ |
| 3 | 18:00 | 03:00 (วันถัดไป) | ข้ามวัน |
| 4 | 19:00 | 03:00 (วันถัดไป) | ข้ามวัน |

### 2. กฎการบันทึกเวลา

- พนักงานต้องบันทึกเวลา **2 ครั้งต่อวัน** (เข้าและออกงาน)
- การบันทึกจะถูกจัดกลุ่มตามวันที่ทำงาน

### 3. กฎพิเศษสำหรับช่วง 02:00 - 03:00 (Overnight Boundary)

```javascript
// กฎการจัดกลุ่มบันทึกเวลาข้ามวัน
if (เวลาบันทึกอยู่ในช่วง 02:00 - 03:00) {
  if (เป็นบันทึกครั้งแรกของวัน) {
    // ถือเป็นเวลาออกงานของวันก่อนหน้า
    วันที่ทำงาน = วันก่อนหน้า
  } else {
    // ถือเป็นเวลาออกงานของวันนั้น
    วันที่ทำงาน = วันปัจจุบัน
  }
}
```

**ตัวอย่าง:**

```
วันที่ 25 มกราคม:
- 18:05 → เข้างาน (วันที่ 25)
- 02:30 (วันที่ 26) → ออกงาน (ยังถือเป็นวันที่ 25)

วันที่ 26 มกราคม:
- 18:05 → เข้างาน (วันที่ 26)
- 02:30 (วันที่ 27) → ออกงาน (ยังถือเป็นวันที่ 26)
```

### 4. การตรวจสอบสถานะ

| สถานะ | เงื่อนไข | ข้อความ |
|-------|----------|---------|
| `COMPLETE` | มีบันทึกเข้าและออกงาน | สมบูรณ์ |
| `MISSING_IN` | ไม่มีบันทึกเข้างาน | ลืมสแกนเข้างาน |
| `MISSING_OUT` | ไม่มีบันทึกออกงาน | ลืมสแกนออกงาน |

---

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง: `master_shift_times`

เก็บข้อมูลช่วงเวลาทำงานหลัก

```sql
CREATE TABLE master_shift_times (
    id SERIAL PRIMARY KEY,
    shift_name VARCHAR(100) NOT NULL,
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    is_overnight BOOLEAN DEFAULT FALSE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### ตาราง: `attendance_logs`

เก็บบันทึกเวลาเข้า-ออกงานดิบ (Raw Data)

```sql
CREATE TABLE attendance_logs (
    id SERIAL PRIMARY KEY,
    employee_id INTEGER NOT NULL REFERENCES employees(id),
    log_time TIMESTAMP WITH TIME ZONE NOT NULL,
    log_type VARCHAR(10) CHECK (log_type IN ('IN', 'OUT')),
    device_id VARCHAR(50),
    location VARCHAR(255),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### ตาราง: `processed_attendance`

เก็บข้อมูลที่ประมวลผลแล้ว

```sql
CREATE TABLE processed_attendance (
    id SERIAL PRIMARY KEY,
    employee_id INTEGER NOT NULL REFERENCES employees(id),
    work_date DATE NOT NULL,
    check_in_time TIMESTAMP WITH TIME ZONE,
    check_out_time TIMESTAMP WITH TIME ZONE,
    shift_id INTEGER REFERENCES master_shift_times(id),
    status VARCHAR(50) DEFAULT 'COMPLETE',
    total_hours DECIMAL(5,2),
    is_overnight BOOLEAN DEFAULT FALSE,
    notes TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    UNIQUE(employee_id, work_date)
);
```

### View: `v_daily_attendance_summary`

View สำหรับแสดงข้อมูลสรุปการเข้างานรายวัน

```sql
CREATE VIEW v_daily_attendance_summary AS
SELECT
    pa.id,
    pa.employee_id,
    e.employee_code,
    e.first_name,
    e.last_name,
    pa.work_date,
    pa.check_in_time,
    pa.check_out_time,
    mst.shift_name,
    pa.status,
    pa.total_hours,
    pa.is_overnight,
    CASE
        WHEN pa.status = 'MISSING_IN' THEN 'ลืมสแกนเข้างาน'
        WHEN pa.status = 'MISSING_OUT' THEN 'ลืมสแกนออกงาน'
        WHEN pa.status = 'COMPLETE' THEN 'สมบูรณ์'
        ELSE pa.status
    END as status_thai
FROM processed_attendance pa
LEFT JOIN employees e ON pa.employee_id = e.id
LEFT JOIN master_shift_times mst ON pa.shift_id = mst.id;
```

---

## 🔌 API Endpoints

### Master Shift Times Management

#### 1. GET `/api/master-times`
ดึงรายการช่วงเวลาทำงานหลักทั้งหมด

**Response:**
```json
{
  "status": "OK",
  "data": [
    {
      "id": 1,
      "shift_name": "กะเช้า 10:00-19:00",
      "start_time": "10:00:00",
      "end_time": "19:00:00",
      "is_overnight": false,
      "is_active": true
    }
  ],
  "count": 4
}
```

#### 2. POST `/api/master-times`
สร้างช่วงเวลาทำงานหลักใหม่

**Request Body:**
```json
{
  "shift_name": "กะเช้า 10:00-19:00",
  "start_time": "10:00:00",
  "end_time": "19:00:00",
  "is_overnight": false
}
```

#### 3. PUT `/api/master-times/:id`
อัพเดทช่วงเวลาทำงานหลัก

#### 4. DELETE `/api/master-times/:id`
ลบช่วงเวลาทำงานหลัก (Soft Delete)

### Attendance Processing

#### 5. POST `/api/master-times/process-attendance`
ประมวลผลบันทึกเวลาเข้า-ออกงาน

**Request Body:**
```json
{
  "employee_id": 1,
  "start_date": "2026-01-20",
  "end_date": "2026-01-26"
}
```

**Response:**
```json
{
  "status": "OK",
  "message": "ประมวลผลข้อมูลสำเร็จ",
  "processed_count": 7,
  "data": [...]
}
```

#### 6. POST `/api/master-times/batch-process`
ประมวลผลบันทึกเวลาสำหรับพนักงานหลายคน

**Request Body:**
```json
{
  "employee_ids": [1, 2, 3],
  "start_date": "2026-01-20",
  "end_date": "2026-01-26"
}
```

### Reporting

#### 7. GET `/api/master-times/report/:employee_id`
สร้างรายงานสรุปการเข้างาน

**Query Parameters:**
- `start_date`: YYYY-MM-DD
- `end_date`: YYYY-MM-DD

**Response:**
```json
{
  "status": "OK",
  "data": {
    "employee_id": 1,
    "period": {
      "start_date": "2026-01-20",
      "end_date": "2026-01-26"
    },
    "summary": {
      "total_days": 7,
      "complete_days": 5,
      "missing_in_days": 1,
      "missing_out_days": 1,
      "total_hours": 45.5,
      "average_hours_per_day": 6.5
    },
    "details": [...]
  }
}
```

#### 8. GET `/api/master-times/processed-attendance`
ดึงข้อมูลการเข้างานที่ประมวลผลแล้ว

**Query Parameters:**
- `employee_id` (optional)
- `start_date` (optional)
- `end_date` (optional)
- `status` (optional): COMPLETE, MISSING_IN, MISSING_OUT

#### 9. GET `/api/master-times/statistics`
สถิติการเข้างานโดยรวม

**Query Parameters:**
- `start_date`: YYYY-MM-DD (required)
- `end_date`: YYYY-MM-DD (required)

**Response:**
```json
{
  "status": "OK",
  "data": {
    "period": {
      "start_date": "2026-01-20",
      "end_date": "2026-01-26"
    },
    "total_records": 100,
    "total_employees": 20,
    "complete_records": 85,
    "missing_in_records": 8,
    "missing_out_records": 7,
    "total_hours": 650.5,
    "average_hours_per_record": 6.51,
    "completion_rate": 85.0
  }
}
```

---

## 🚀 การติดตั้งและใช้งาน

### 1. ติดตั้ง Dependencies

```bash
cd /Users/m/ไดรฟ์ของฉัน/vue3-app
npm install
```

### 2. สร้าง Database Schema

```bash
# เชื่อมต่อกับ Supabase และรัน migration
psql -h <supabase-host> -U postgres -d postgres -f migrations/001_create_master_times_schema.sql
```

หรือใช้ Supabase Dashboard:
1. ไปที่ SQL Editor
2. Copy เนื้อหาจาก `migrations/001_create_master_times_schema.sql`
3. Run SQL

### 3. ตั้งค่า Environment Variables

ตรวจสอบไฟล์ `.env`:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### 4. เริ่มต้นใช้งาน

```bash
# Start development server
npm run dev:all
```

### 5. เข้าถึง UI

เปิดเบราว์เซอร์และไปที่:
```
http://localhost:5173/master-times
```

---

## 🧪 การทดสอบ

### รันการทดสอบทั้งหมด

```bash
npm run test
```

### รันการทดสอบเฉพาะ Master Times

```bash
npm run test tests/masterTimes.test.js
```

### Test Coverage

ชุดทดสอบครอบคลุม:

✅ **Overnight Boundary Detection** (6 test cases)
- ตรวจจับเวลาในช่วง 02:00-03:00

✅ **Work Date Calculation** (4 test cases)
- คำนวณวันที่ทำงานสำหรับบันทึกครั้งแรกและครั้งที่สอง
- จัดการเวลาปกติและเวลาข้ามวัน

✅ **Log Grouping** (3 test cases)
- จัดกลุ่มบันทึกตามวันที่ทำงาน
- รองรับกะปกติและกะข้ามวัน

✅ **Shift Matching** (6 test cases)
- จับคู่ Shift ที่เหมาะสมจากเวลาเข้างาน
- ยอมรับความแตกต่าง ±2 ชั่วโมง

✅ **Work Hours Calculation** (4 test cases)
- คำนวณชั่วโมงทำงานสำหรับกะปกติและกะข้ามวัน
- จัดการกรณีขาดข้อมูล

✅ **Work Day Processing** (3 test cases)
- ประมวลผลวันทำงานที่สมบูรณ์
- ตรวจจับการลืมสแกน

✅ **Edge Cases** (4 test cases)
- บันทึกมากกว่า 2 ครั้งต่อวัน
- เวลาตรงเที่ยงคืน, 02:00, 03:00

✅ **Complex Scenarios** (2 test cases)
- กะข้ามวันติดต่อกัน
- กะหลายประเภทในสัปดาห์เดียวกัน

---

## 💡 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: กะเช้าปกติ (10:00-19:00)

**Input:**
```javascript
const logs = [
  { employee_id: 1, log_time: '2026-01-26T10:05:00' },
  { employee_id: 1, log_time: '2026-01-26T19:02:00' }
];
```

**Output:**
```javascript
{
  work_date: '2026-01-26',
  check_in_time: '2026-01-26T10:05:00',
  check_out_time: '2026-01-26T19:02:00',
  shift_id: 1,
  status: 'COMPLETE',
  total_hours: 8.95,
  is_overnight: false
}
```

### ตัวอย่างที่ 2: กะดึกข้ามวัน (18:00-03:00)

**Input:**
```javascript
const logs = [
  { employee_id: 1, log_time: '2026-01-25T18:05:00' },
  { employee_id: 1, log_time: '2026-01-26T02:55:00' }
];
```

**Output:**
```javascript
{
  work_date: '2026-01-25',  // ทั้งสองบันทึกถือเป็นวันที่ 25
  check_in_time: '2026-01-25T18:05:00',
  check_out_time: '2026-01-26T02:55:00',
  shift_id: 3,
  status: 'COMPLETE',
  total_hours: 8.83,
  is_overnight: true
}
```

### ตัวอย่างที่ 3: ลืมสแกนออกงาน

**Input:**
```javascript
const logs = [
  { employee_id: 1, log_time: '2026-01-26T10:05:00' }
];
```

**Output:**
```javascript
{
  work_date: '2026-01-26',
  check_in_time: '2026-01-26T10:05:00',
  check_out_time: null,
  shift_id: 1,
  status: 'MISSING_OUT',
  total_hours: null,
  is_overnight: false
}
```

### ตัวอย่างที่ 4: การใช้งาน API

```javascript
// ประมวลผลบันทึกเวลา
const response = await fetch('http://localhost:3000/api/master-times/process-attendance', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    employee_id: 1,
    start_date: '2026-01-20',
    end_date: '2026-01-26'
  })
});

const result = await response.json();
console.log(result);
// {
//   status: 'OK',
//   message: 'ประมวลผลข้อมูลสำเร็จ',
//   processed_count: 7,
//   data: [...]
// }
```

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: บันทึกเวลาไม่ถูกจัดกลุ่มถูกต้อง

**สาเหตุ:** เวลาในช่วง 02:00-03:00 ไม่ได้รับการประมวลผลถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่า timezone ของ database ตรงกับ timezone ของ application
2. ตรวจสอบฟังก์ชัน `calculateWorkDate()` ใน `masterTimesService.js`

### ปัญหา: Shift ไม่ถูกจับคู่

**สาเหตุ:** เวลาเข้างานห่างจาก Shift มากกว่า 2 ชั่วโมง

**วิธีแก้:**
1. ปรับค่า tolerance ในฟังก์ชัน `findBestMatchingShift()`
2. สร้าง Shift ใหม่ที่ตรงกับเวลาเข้างานจริง

---

## 📊 Performance Optimization

### Database Indexes

ระบบได้สร้าง indexes เพื่อเพิ่มประสิทธิภาพ:

```sql
CREATE INDEX idx_attendance_logs_employee_time
    ON attendance_logs(employee_id, log_time DESC);

CREATE INDEX idx_processed_attendance_employee_date
    ON processed_attendance(employee_id, work_date DESC);
```

### Caching Strategy

แนะนำให้ใช้ Redis cache สำหรับ:
- Master shift times (TTL: 1 hour)
- Processed attendance (TTL: 5 minutes)

---

## 🔐 Security Considerations

1. **Input Validation:** ทุก API endpoint มี validation
2. **SQL Injection Prevention:** ใช้ Supabase client (parameterized queries)
3. **Authentication:** ควรเพิ่ม JWT authentication สำหรับ production
4. **Rate Limiting:** ควรเพิ่ม rate limiting สำหรับ batch processing

---

## 📝 License

MIT License

---

## 👥 Contributors

- Development Team
- QA Team
- Product Owner

---

## 📞 Support

หากพบปัญหาหรือมีคำถาม กรุณาติดต่อ:
- Email: support@example.com
- Issue Tracker: GitHub Issues

---

**เอกสารนี้อัพเดทล่าสุด:** 26 มกราคม 2026
