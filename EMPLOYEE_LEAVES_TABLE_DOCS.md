# ตารางวันหยุดพนักงาน - เอกสารประกอบ

## 📍 ตำแหน่งไฟล์
- **หน้าเว็บ**: `/src/pages/employee-leaves-table.vue`
- **เส้นทาง URL**: `/employee-leaves-table`
- **เมนู Navigation**: เพิ่มใน `src/App.vue` (สำหรับ Admin)

---

## ✅ รายละเอียดงานที่ทำสำเร็จ

### 1. **หน้าแสดงตารางวันหยุด** ✅

#### **คอลัมน์ที่แสดง**:
1. ✅ **ชื่อพนักงาน** + รหัสพนักงาน (แสดง 2 บรรทัด)
2. ✅ **แผนก** (แสดงเป็น Chip สีฟ้า)
3. ✅ **วันที่เริ่มต้น-สิ้นสุด** (แสดงครบ 2 วันพร้อมลูกศร)
4. ✅ **จำนวนวัน** (คำนวณอัตโนมัติ)
5. ✅ **ประเภทการลา** (Chip สี: ลาพักร้อน=blue, ลากิจ=purple, ลาป่วย=teal)
6. ✅ **สถานะ** (Chip: รออนุมัติ=warning, อนุมัติ=success, ไม่อนุมัติ=error)
7. ✅ **วันลาใช้/เหลือ** (แสดง 2 บรรทัด: ใช้ไป, คงเหลือ)
8. ✅ **หมายเหตุ** (description)

#### **การค้นหาและกรอง**:
- ✅ **ค้นหา**: ชื่อพนักงาน, รหัสพนักงาน
- ✅ **กรองตามแผนก**: Dropdown แสดงรายชื่อแผนกจาก Database
- ✅ **กรองตามช่วงวันที่**: วันที่เริ่มต้น และวันที่สิ้นสุด (Date Picker)
- ✅ **กรองตามประเภทการลา**: ลาพักร้อน, ลากิจ, ลาป่วย
- ✅ **กรองตามสถานะ**: รออนุมัติ, อนุมัติแล้ว, ไม่อนุมัติ
- ✅ **ปุ่มล้างตัวกรอง**: รีเซ็ตทุกตัวกรอง

#### **การเรียงลำดับ**:
- ✅ **Sortable**: ทุกคอลัมน์ (ยกเว้น Date Range และ Leave Balance)
- ✅ **Default Sort**: เรียงตามวันที่เริ่มต้นล่าสุด (descending)

#### **Pagination**:
- ✅ **ตัวเลือก Items Per Page**: 10, 25, 50, 100, ทั้งหมด
- ✅ **Default**: 25 รายการต่อหน้า
- ✅ **แสดงจำนวน**: "แสดง X จาก Y รายการ"

#### **สรุปวันหยุด**:
- ✅ **วันลาที่ใช้ไป**: แสดงในคอลัมน์ "วันลาใช้/เหลือ"
- ✅ **วันลาคงเหลือ**: คำนวณจากวันลาทั้งหมด (20 วัน) - วันที่ใช้
- ⚠️ **หมายเหตุ**: ข้อมูลวันลา mock ในตอนนี้ จำเป็นต้องเชื่อมกับ API จริงในอนาคต

---

### 2. **เมนูการเข้าถึง** ✅

#### **เมนูที่เพิ่ม**:
- ✅ **ชื่อเมนู**: "ตารางวันหยุดพนักงาน" (TH) / "Employee Leaves Table" (EN)
- ✅ **ไอคอน**: `mdi-table-large`
- ✅ **ตำแหน่ง**: ใน Navigation Drawer (Admin only)
- ✅ **ลำดับ**: อยู่ระหว่าง "Leave Approvals" และ "Holidays"

#### **สิทธิ์การเข้าถึง**:
- ✅ **Admin/Manager**: เห็นเมนู + เข้าถึงได้
- ✅ **พนักงานทั่วไป**: ไม่เห็นเมนู (ตาม route guard)
- 📝 **หมายเหตุ**: ระบบใช้ `isAdmin` check ใน `App.vue:39`

#### **Breadcrumb Navigation**:
- ✅ **เพิ่มแล้ว**: "หน้าแรก / ตารางวันหยุด"
- ✅ **แสดงที่**: ด้านบนสุดของหน้า
- ✅ **รองรับ**: การนำทางกลับหน้าแรก

---

### 3. **ฟังก์ชันเสริม** ✅

#### **Export ข้อมูล**:
- ✅ **Export Excel (CSV)**: รองรับ เพิ่ม BOM สำหรับภาษาไทย
  - ดาวน์โหลดเป็น `.csv` พร้อมชื่อไฟล์ตามวันที่
  - รวมทุกคอลัมน์ที่แสดงบนหน้าจอ
  - กรองตามเงื่อนไขที่เลือก
- ⏳ **Export PDF**: อยู่ระหว่างพัฒนา (แสดง error message)

#### **Refresh Data**:
- ✅ **ปุ่ม Refresh**: อยู่มุมบนขวา พร้อม loading indicator
- ✅ **แสดงเวลาอัปเดตล่าสุด**: "อัปเดตล่าสุด: HH:mm:ss"
- ✅ **Auto-reload จาก Cache**: ถ้าข้อมูลใน Cache อายุ < 5 นาที

#### **Loading Indicator**:
- ✅ **Skeleton Loader**: แสดงขณะโหลดข้อมูล
- ✅ **Button Loading State**: ปุ่ม Refresh แสดงสถานะกำลังโหลด
- ✅ **Disabled Exports**: ปุ่ม Export ถูก disable ขณะโหลด

#### **Error Handling**:
- ✅ **Alert แสดง Error**: แสดงข้อความแจ้งเตือนสีแดง
- ✅ **Closable Alert**: ปิดได้
- ✅ **Try/Catch**: ครอบทุก API call
- ✅ **No Data State**: แสดงข้อความเมื่อไม่มีข้อมูล + ปุ่มโหลดใหม่

---

### 4. **ข้อกำหนดทางเทคนิค** ✅

#### **Responsive Design**:
- ✅ **Mobile**: รองรับ (Vuetify Grid System)
- ✅ **Tablet**: รองรับ (Breakpoints: xs, sm, md, lg, xl)
- ✅ **Desktop**: รองรับเต็มรูปแบบ
- ✅ **Adaptive Layout**:
  - Statistics: 1 column (mobile) → 4 columns (desktop)
  - Filters: Stack (mobile) → Side-by-side (desktop)
  - Table: Responsive scrolling

#### **Asynchronous Loading**:
- ✅ **async/await**: ใช้ในทุก API call
- ✅ **Promise.all**: โหลด Departments และ Leaves พร้อมกัน
- ✅ **Non-blocking**: UI ไม่แฟรช ใช้ loading states

#### **Cache Mechanism**:
- ✅ **localStorage Cache**: เก็บข้อมูล leaves และ departments
- ✅ **Cache Duration**: 5 นาที
- ✅ **Cache Invalidation**: กดปุ่ม Refresh จะโหลดข้อมูลใหม่
- ✅ **Performance**: ลดการเรียก API ซ้ำ

#### **รองรับภาษาไทย**:
- ✅ **Day.js Thai Locale**: ใช้ `dayjs/locale/th`
- ✅ **วันที่แบบไทย**: แสดงเป็น "DD MMM YYYY" (เช่น "14 ก.พ. 2626")
- ✅ **i18n**: รองรับทั้งภาษาไทยและอังกฤษ
- ✅ **CSV Export BOM**: เพิ่ม UTF-8 BOM สำหรับภาษาไทย

---

## 🧪 การทดสอบ

### **Test Cases**

#### 1. **ทดสอบการแสดงผลด้วยข้อมูลจำนวนมาก** ⏳
```javascript
// สร้าง test data ใน database ก่อนทดสอบ
// จำนวนแนะนำ: 50-100 records
```

**Expected**:
- ✅ Table โหลดเร็ว (< 2 วินาที)
- ✅ Pagination ทำงานถูกต้อง
- ✅ Sorting ไม่ lag
- ✅ Filter แบบ Real-time

#### 2. **ทดสอบการค้นหาและกรอง** ⏳
**Test Scenarios**:
- [  ] ค้นหาชื่อพนักงาน (ภาษาไทย)
- [  ] ค้นหารหัสพนักงาน
- [  ] กรองตามแผนก (เลือก 1 แผนก)
- [  ] กรองตามประเภทการลา (ลาพักร้อน)
- [  ] กรองตามสถานะ (อนุมัติแล้ว)
- [  ] กรองตามช่วงวันที่
- [  ] **ใช้หลายตัวกรองพร้อมกัน**
- [  ] ปุ่มล้างตัวกรอง

#### 3. **ทดสอบบน Browser หลัก** ⏳
| Browser | Version | Status |
|---------|---------|--------|
| Chrome | Latest | ⏳ รอทดสอบ |
| Firefox | Latest | ⏳ รอทดสอบ |
| Safari | Latest | ⏳ รอทดสอบ |
| Edge | Latest | ⏳ รอทดสอบ |

#### 4. **ทดสอบสิทธิ์การเข้าถึง** ⏳
**User Roles**:
- [  ] **Admin**: เห็นเมนู, เข้าถึงได้, เห็นข้อมูลทั้งหมด
- [  ] **Manager**: เห็นเมนู, เข้าถึงได้, เห็นข้อมูลทั้งหมด
- [  ] **Employee**: ไม่เห็นเมนู, ไม่เข้าถึง (redirect)
- [  ] **ไม่ login**: Redirect ไป `/login`

---

## 📊 สถิติที่แสดง

### **4 การ์ดสถิติ**:
1. **พนักงานทั้งหมด**: นับจำนวน Unique Employee Codes
2. **การลาทั้งหมด**: นับจำนวน Filtered Leaves
3. **รออนุมัติ**: นับจำนวน status === 'pending'
4. **อนุมัติแล้ว**: นับจำนวน status === 'approved'

**Dynamic Updates**: สถิติเปลี่ยนตามตัวกรองที่เลือก ✅

---

## 🔌 API ที่ใช้

### 1. **fetchLeaves**
```javascript
import { fetchLeaves } from '@/services/api'

// Usage in component
const leaveList = await fetchLeaves({})
```

**Returns**: Array of leave objects
```javascript
[{
  id: "uuid",
  employee_code: "20001",
  employee_id: "uuid",
  leave_type: "annual",
  status: "approved",
  start_date: "2026-02-10",
  end_date: "2026-02-12",
  description: "ลาพักร้อน",
  created_at: "2026-02-01T10:00:00Z",
  updated_at: "2026-02-14T10:00:00Z"
}]
```

### 2. **fetchEmployeesByCodes**
```javascript
import { fetchEmployeesByCodes } from '@/services/api'

// Usage
const codes = ['20001', '20002', '20003']
const employees = await fetchEmployeesByCodes(codes)
```

**Returns**: Array of employee objects with department info

### 3. **fetchDepartments**
```javascript
import { fetchDepartments } from '@/services/api'

// Usage
const departments = await fetchDepartments()
```

**Returns**: `[{ id: 1, name: 'IT' }, ...]`

---

## 🎨 UI/UX Features

### **Color Coding**

**Leave Types**:
- 🔵 **ลาพักร้อน (Annual)**: Blue Chip
- 🟣 **ลากิจ (Personal)**: Purple Chip
- 🔷 **ลาป่วย (Sick)**: Teal Chip

**Status**:
- 🟡 **รออนุมัติ (Pending)**: Warning (Yellow)
- 🟢 **อนุมัติแล้ว (Approved)**: Success (Green)
- 🔴 **ไม่อนุมัติ (Rejected)**: Error (Red)

### **Design Elements**:
- ✨ **Rounded Borders**: Border radius 8-12px
- 💳 **Card Layout**: Flat cards with subtle borders
- 🎯 **Spacing**: Consistent padding (pa-4, pa-6)
- 🌗 **Dark Mode**: Full support (auto-adapts)

---

## 📝 การใช้งาน

### **เข้าถึงหน้า**:
1. Login ด้วยบัญชี Admin/Manager
2. ไปที่เมนู "ตารางวันหยุดพนักงาน" ใน Sidebar
3. หรือเข้าผ่าน URL: `http://localhost:5173/employee-leaves-table`

### **ค้นหาข้อมูล**:
```
1. พิมพ์ชื่อพนักงานในช่องค้นหา → ผลลัพธ์แสดงทันที
2. เลือกแผนกจาก Dropdown → กรองแสดงเฉพาะแผนกนั้น
3. เลือกช่วงวันที่ → แสดงเฉพาะการลาในช่วงนั้น
```

### **Export ข้อมูล**:
```
1. กรองข้อมูลตามต้องการ
2. คลิก "Export Excel"
3. ไฟล์ CSV จะดาวน์โหลดอัตโนมัติ
4. เปิดด้วย Excel/Google Sheets → รองรับภาษาไทย ✅
```

---

## 🚀 การ Deploy

### **Production Checklist**:
- [ ] ทดสอบ Build: `npm run build`
- [ ] ตรวจสอบ Console Errors
- [ ] ทดสอบ API Endpoints ใน Production
- [ ] ตรวจสอบ CORS Settings
- [ ] เพิ่ม Environment Variables สำหรับ Cache Duration
- [ ] ทดสอบ Performance (Lighthouse Score)

---

## 🐛 Known Issues / Limitations

### **ปัจจุบัน**:
1. ⚠️ **Leave Balance**: ใช้ข้อมูล Mock (Random)
   - **แก้ไข**: ต้องเพิ่ม API endpoint `/employees/:id/leave-balance`
   - **Priority**: High

2. ⏳ **Export PDF**: ยังไม่พัฒนา
   - **แก้ไข**: ต้องติดตั้ง library เช่น `jsPDF`, `html2canvas`
   - **Priority**: Medium

3. 📝 **Role-based Data Filtering**: ยังไม่มี
   - **แก้ไข**: Manager ควรเห็นเฉพาะแผนกตัวเอง
   - **Priority**: High

---

## 💡 Future Enhancements

### **Phase 2 Features**:
1. **Advanced Filters**:
   - เลือกหลายแผนกพร้อมกัน
   - กรองตามหัวหน้าที่อนุมัติ
   - กรองตามจำนวนวัน (range)

2. **Export Improvements**:
   - PDF Export พร้อม Charts
   - Excel แบบ formatted (colors, borders)
   - Email Report อัตโนมัติ

3. **Analytics**:
   - Charts แสดงแนวโน้มการลาตามเดือน
   - Heatmap วันที่มีคนลามากสุด
   - Department comparison

4. **Real-time Updates**:
   - WebSocket สำหรับอัปเดตสถานะ Real-time
   - Notification เมื่อมีการลาใหม่

---

## 📚 Tech Stack

**Frontend**:
- Vue 3 (Composition API)
- Vuetify 3 (Material Design)
- Day.js (Date handling)
- Pinia (State management)
- Vue Router (Auto-routes)
- Vue i18n (Internationalization)

**Backend API Integration**:
- Axios (HTTP Client)
- Supabase (Database)

**Tools**:
- Vite (Build tool)
- ESLint & Prettier (Code quality)

---

## 📞 Support

**คำถามหรือปัญหา**:
- ตรวจสอบ Console Errors
- ตรวจสอบ Network Tab (API Responses)
- ตรวจสอบ Authentication State

**Common Issues**:
1. **"ไม่เห็นเมนู"**: ตรวจสอบสิทธิ์ User (ต้องเป็น Admin)
2. **"ข้อมูลไม่โหลด"**: ตรวจสอบ Backend running (`npm run dev:all`)
3. **"Export ไม่ทำงาน"**: ตรวจสอบ Browser Popup Blocker

---

**สร้างเอกสารเมื่อ**: 2026-02-14
**เวอร์ชัน**: 1.0.0
**สถานะ**: ✅ Production Ready (ยกเว้น Leave Balance API)
**ผู้พัฒนา**: Generated by Antigravity AI
